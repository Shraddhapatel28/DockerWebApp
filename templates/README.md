# 🌐 Dockerized Flask Web App

This project demonstrates a simple **Flask web application** running inside a **Docker container**.

## 🧾 Description
This web app shows how to containerize a Python Flask project using **Docker** and **Docker Compose**.  
It displays a simple webpage with navigation links like Home, About, and Contact.

## 🛠️ Technologies Used
- Python (Flask)
- HTML, CSS
- Docker & Docker Compose

## 🚀 How to Run the Project
1. Open terminal in project folder.
2. Run the following command:
   ```bash
   docker-compose up --build
